import { home } from './views/home.js';
 
export const manageRoute = (hash) => {
 if( hash === '#/home'){
     return showPage(hash)
 else if ( hash === )    
 }

const showPage(hash)


{
  swicht (hash)
  window.location
}
}


